using System;

// Members throughout this assembly are marked [CLSCompliant(false)] -- the unsigned-integer
// overloads of the assertion helpers, mostly -- which the compiler warns about (CS3021) unless
// the assembly itself declares compliance. Declaring it is what those attributes were written
// to expect.
[assembly: CLSCompliant(true)]
